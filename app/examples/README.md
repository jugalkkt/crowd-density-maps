# Demo examples

Drop a few ShanghaiTech test images (`.jpg` / `.png`) in this folder and they
show up as one-click examples in the Gradio demo. Part B test images make the
best first impression — the counts are small enough to check by eye.

This folder is committed so the demo has somewhere to look; the images
themselves are not, since they belong to the ShanghaiTech dataset.
